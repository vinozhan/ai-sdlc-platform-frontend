/**
 * Visualization wrappers.
 * Mermaid is lazy-loaded. React Flow / Recharts stay feature-owned until a
 * second consumer appears (2+ promotion rule).
 */
export { MermaidDiagram } from "./MermaidDiagram";
